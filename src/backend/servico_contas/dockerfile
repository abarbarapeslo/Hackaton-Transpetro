# 1. Usar uma imagem base oficial do Python. A versão 'slim' é mais leve.
FROM python:3.10-slim

# 2. Instalar o cliente PostgreSQL (necessário para 'pg_isready')
# 'apt-get update' é necessário em imagens Debian/Slim antes de instalar pacotes.
RUN apt-get update && \
    apt-get install -y postgresql-client && \
    rm -rf /var/lib/apt/lists/*

# 3. Definir o diretório de trabalho dentro do container
WORKDIR /app

# 4. Copiar o arquivo de dependências PRIMEIRO
COPY requirements.txt .

# 5. Instalar as dependências (incluindo psycopg2-binary, se já não estiver lá)
RUN pip install --no-cache-dir -r requirements.txt

# 6. Copiar o resto do código da aplicação para o diretório de trabalho
COPY . .
